
// Animation Directions
enum Animation {
   AnimationForward = 1,
   AnimationBack = -1
};

// Limb Directions
enum Limb {
   LimbLeft = -1,
   LimbRight = 1
};

// Point Structure
typedef struct {
   int x;
   int y;
} Point;

